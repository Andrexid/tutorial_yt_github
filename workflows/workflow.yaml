name: Esempio GitHub Actions

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Stampa un messaggio
        run: echo "GitHub Actions è attivo!"
